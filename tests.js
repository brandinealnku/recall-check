(() => { "use strict"; const frame = document.getElementById("app"); const list = document.getElementById("tests"); let passed = 0; let failed = 0; function report(name, ok, detail = "") { const item = document.createElement("li"); item.textContent = `${ok ? "PASS" : "FAIL"}: ${name}${detail ? ` — ${detail}` : ""}`; item.style.color = ok ? "#17613f" : "#a1282f"; list.append(item); ok ? passed++ : failed++; } function assert(name, condition) { report(name, Boolean(condition)); } frame.addEventListener("load", async () => { const api = frame.contentWindow.RecallCheck; assert("UPC normalization", api.normalizeBarcode("0123-456 78905") === "012345678905"); const variants = api.generateGtinVariants("012345678905"); assert("GTIN variant generation", variants.includes("00012345678905") && variants.includes("0012345678905")); assert("Valid check digit", api.validateCheckDigit("012345678905")); assert("Invalid check digit", !api.validateCheckDigit("012345678904")); const recall = { upcs: ["012345678905"], gtins: [], lotCodes: ["L2407A"], dateCodes: [] }; assert("Exact recall matching", api.exactCodeMatch("00012345678905", recall)); assert("Lot-code matching", api.lotCodeMatches("l2407a", recall)); assert("No-match behavior", api.classifyResult({ code: "737628064502", product_name: "Rice", brands: "Other", quantity: "4 oz" }, [recall]).status === "no_matching_recall"); assert("Malformed recall data", !api.validateRecallData({ generatedAt: "now", recalls: "bad" })); const missing = await api.identifyProductWithOpenFoodFacts("12345670", { demo: { product: null } }); assert("Missing product data", missing === null); try { await api.identifyProductWithOpenFoodFacts("12345670", { timeout: 5, fetchImpl: (_url, opts) => new Promise((_resolve, reject) => opts.signal.addEventListener("abort", () => reject(new DOMException("Aborted", "AbortError")))) }); report("API timeout handling", false); } catch (error) { report("API timeout handling", error.message === "timeout", error.message); } const node = frame.contentDocument.createElement("p"); node.textContent = "<img src=x onerror=alert(1)>"; assert("Safe DOM rendering", node.children.length === 0 && node.textContent.startsWith("<img")); document.getElementById("summary").textContent = `${passed} passed, ${failed} failed`; document.getElementById("summary").className = failed ? "field-error" : "notice"; }); })();
